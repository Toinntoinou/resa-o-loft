# ─── Base de données ───────────────────────────────────────────────
# En local : SQLite (aucune installation requise)
DATABASE_URL="file:./dev.db"
# En production (Vercel/Neon) : remplacer par une URL Postgres, ex :
# DATABASE_URL="postgresql://user:pass@host/dbname?sslmode=require"

# ─── Accès administrateur ──────────────────────────────────────────
# Mot de passe pour se connecter à /admin. À CHANGER.
ADMIN_PASSWORD="loft2026"
# Clé secrète pour signer la session admin (chaîne aléatoire longue). À CHANGER.
SESSION_SECRET="remplacez-moi-par-une-longue-chaine-aleatoire"

# ─── Emails de confirmation (Resend) ───────────────────────────────
# Laisser vide pour désactiver l'envoi d'emails (la résa fonctionne quand même).
# Créer un compte gratuit sur https://resend.com puis coller la clé ici.
RESEND_API_KEY=""
# Expéditeur des emails. Tant que votre domaine n'est pas vérifié dans Resend,
# utilisez l'adresse de test "onboarding@resend.dev".
EMAIL_FROM="Le Loft <onboarding@resend.dev>"
# Adresse qui reçoit une copie/notification des nouvelles réservations (optionnel).
EMAIL_ADMIN_NOTIFY=""

# ─── Identité de l'espace (affiché sur le site) ────────────────────
NEXT_PUBLIC_SITE_NAME="Le Loft"
NEXT_PUBLIC_SITE_TAGLINE="Espace de coworking"
